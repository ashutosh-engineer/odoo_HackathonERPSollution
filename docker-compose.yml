# ============================================================
# Shiv Furniture Works ERP — Complete Docker Stack
# Odoo 16 + PostgreSQL 15 + Redis 7 + Nginx + PgBouncer
# Local mode: HTTP only (no SSL certificate required)
# ============================================================

services:

  # ──────────────────────────────────────────────
  # PostgreSQL 15 — Primary Database
  # ──────────────────────────────────────────────
  db:
    image: postgres:15-alpine
    container_name: shiv_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: shiv_furniture_erp
      POSTGRES_USER: shiv_odoo
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-ShivSecure@2024}
      POSTGRES_INITDB_ARGS: "--encoding=UTF8"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/postgres/init:/docker-entrypoint-initdb.d:ro
    ports:
      - "5432:5432"
    networks:
      - shiv_net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U shiv_odoo -d shiv_furniture_erp"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    command: >
      postgres
        -c max_connections=200
        -c shared_buffers=256MB
        -c effective_cache_size=768MB
        -c maintenance_work_mem=64MB
        -c checkpoint_completion_target=0.9
        -c wal_buffers=16MB
        -c default_statistics_target=100
        -c random_page_cost=1.1
        -c work_mem=4MB
        -c min_wal_size=1GB
        -c max_wal_size=4GB
        -c log_min_duration_statement=5000
        -c log_checkpoints=on
        -c log_connections=on
        -c log_disconnections=on
        -c log_lock_waits=on
        -c deadlock_timeout=1s

  # ──────────────────────────────────────────────
  # PgBouncer — Connection Pooler
  # 1000 client connections → 25 actual DB connections
  # ──────────────────────────────────────────────
  pgbouncer:
    image: edoburu/pgbouncer:1.21.0
    container_name: shiv_pgbouncer
    restart: unless-stopped
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_USER: shiv_odoo
      DB_PASSWORD: ${POSTGRES_PASSWORD:-ShivSecure@2024}
      DB_NAME: shiv_furniture_erp
      POOL_MODE: transaction
      MAX_CLIENT_CONN: 1000
      DEFAULT_POOL_SIZE: 25
      MIN_POOL_SIZE: 5
      RESERVE_POOL_SIZE: 5
      RESERVE_POOL_TIMEOUT: 3
      SERVER_IDLE_TIMEOUT: 600
      CLIENT_IDLE_TIMEOUT: 0
      LOG_CONNECTIONS: 1
      LOG_DISCONNECTIONS: 1
      LOG_POOLER_ERRORS: 1
      ADMIN_USERS: shiv_odoo
    ports:
      - "6432:5432"
    networks:
      - shiv_net
    depends_on:
      db:
        condition: service_healthy

  # ──────────────────────────────────────────────
  # Redis 7 — Sessions, Cache, Distributed Locks
  # ──────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: shiv_redis
    restart: unless-stopped
    command: >
      redis-server
        --requirepass ${REDIS_PASSWORD:-ShivRedis@2024}
        --maxmemory 256mb
        --maxmemory-policy allkeys-lru
        --save 900 1
        --save 300 10
        --appendonly yes
        --appendfsync everysec
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"
    networks:
      - shiv_net
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD:-ShivRedis@2024}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ──────────────────────────────────────────────
  # Odoo 16 — ERP Application Server
  # Connects to DB via PgBouncer (port 5432 on pgbouncer container)
  # ──────────────────────────────────────────────
  odoo:
    build:
      context: .
      dockerfile: docker/odoo/Dockerfile
    container_name: shiv_odoo
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      pgbouncer:
        condition: service_started
    environment:
      HOST: pgbouncer
      PORT: "5432"
      USER: shiv_odoo
      PASSWORD: ${POSTGRES_PASSWORD:-ShivSecure@2024}
      REDIS_URL: redis://:${REDIS_PASSWORD:-ShivRedis@2024}@redis:6379/0
      ODOO_ADMIN_PASSWD: ${ODOO_MASTER_PASSWORD:-ShivMaster@2024}
    volumes:
      - ./backend:/mnt/extra-addons
      - odoo_data:/var/lib/odoo
      - ./docker/odoo/odoo.conf:/etc/odoo/odoo.conf:ro
    ports:
      - "8069:8069"
      - "8072:8072"
    networks:
      - shiv_net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8069/web/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 90s

  # ──────────────────────────────────────────────
  # Nginx — Reverse Proxy (HTTP only, no SSL locally)
  # SSL can be enabled when certificate is available
  # ──────────────────────────────────────────────
  nginx:
    image: nginx:1.25-alpine
    container_name: shiv_nginx
    restart: unless-stopped
    depends_on:
      - odoo
    ports:
      - "80:80"
    volumes:
      - ./docker/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./docker/nginx/conf.d:/etc/nginx/conf.d:ro
      - nginx_logs:/var/log/nginx
    networks:
      - shiv_net

  # ──────────────────────────────────────────────
  # Adminer — DB Admin UI (dev profile only)
  # Start with: docker-compose --profile dev up
  # ──────────────────────────────────────────────
  adminer:
    image: adminer:4.8.1
    container_name: shiv_adminer
    restart: unless-stopped
    ports:
      - "8080:8080"
    networks:
      - shiv_net
    depends_on:
      - db
    environment:
      ADMINER_DEFAULT_SERVER: db
      ADMINER_DESIGN: pepa-linha
    profiles:
      - dev

# ============================================================
# Volumes
# ============================================================
volumes:
  postgres_data:
    driver: local
    name: shiv_postgres_data
  redis_data:
    driver: local
    name: shiv_redis_data
  odoo_data:
    driver: local
    name: shiv_odoo_data
  nginx_logs:
    driver: local
    name: shiv_nginx_logs

# ============================================================
# Networks — single flat network for local dev
# (split internal/external with firewall rules in production)
# ============================================================
networks:
  shiv_net:
    driver: bridge
    name: shiv_net
